# Teable OAuth — 從 Teable workspace > Settings > OAuth Apps 取得
TEABLE_CLIENT_ID=
TEABLE_CLIENT_SECRET=
TEABLE_REDIRECT_URI=

# Teable Personal API Token（代替 OAuth flow，用於 server-to-server）
TEABLE_API_TOKEN=
